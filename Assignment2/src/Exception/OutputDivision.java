package Exception;

public class OutputDivision {
    public static void main(String[] args) {
        Divison obj = new Divison();

        //MathDivision gives try, catch and finally
        obj.MathDivision();

        //Array is used for throws
        obj.Array();

        //Arr is used for throw where simple code is written
        obj.Arr();

    }
}
